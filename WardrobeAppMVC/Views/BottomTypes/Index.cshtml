@model IEnumerable<WardrobeAppMVC.Models.BottomType>

@{
    ViewBag.Title = "Index";
}
<div class="index">
    <div class="container">
        <h2>Index</h2>

        <p>
            @Html.ActionLink("Create New", "Create")
        </p>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.BottomType1)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.BottomType1)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.BottomTypeID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.BottomTypeID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.BottomTypeID })
                </td>
            </tr>
    }

        </table>
        <div class="empty-row"></div>
    </div>
</div>